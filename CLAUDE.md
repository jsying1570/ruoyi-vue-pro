# 项目规范

本项目使用 OpenSpec 管理规范，处理任何任务前必须先读取以下规范文件：

## 必读规范
- openspec/specs/project-structure/    # 项目结构
- openspec/specs/naming-conventions/   # 命名规范
- openspec/specs/layered-architecture/ # 分层架构
- openspec/specs/api-spec/             # API 规范
- openspec/specs/data-object-pattern/  # 数据对象模式
- openspec/specs/permission-model/     # 权限模型
- openspec/specs/ruoyi-conventions/    # ruoyi 整体约定

## 规则
1. 生成代码前先确认符合以上规范
2. 新功能参考 openspec/changes/ 下的对应 change 文档
3. 不确定规范时主动读取相关 spec 文件